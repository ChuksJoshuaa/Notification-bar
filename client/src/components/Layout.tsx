import { Navbar, Notification } from "@/components";

const Layout = () => {
  return (
    <div className="layout_container mt-10">
      <Navbar />
      <Notification />
    </div>
  );
};

export default Layout;
